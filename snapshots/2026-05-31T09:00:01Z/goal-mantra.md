# GOAL MANTRA -- Perpetual Directive

## The grid is the work. The walkway is the path. The vault is the record. The repo is the banner.

### READ walkway (in order)
  1. state.md        ~/.hermes/mind-palace/state.md
  2. plan.md         ~/.hermes/mind-palace/plan.md
  3. money-room-vault.md  ~/.hermes/mind-palace/money-room-vault.md
  4. goal-mantra.md  ~/.hermes/goal-mantra.md
  5. payout_ledger.py ~/.hermes/mind-palace/payout_ledger.py

### UPDATE memory when:
- User corrects an approach or preference -> memory add/replace
- A new workflow pattern emerges -> skill_manage create
- A C source or tool quirk gets discovered -> memory add
- A convention gets repeated -> memory add
- Skill is stale/wrong -> skill_manage patch

### PICK
Lowest undone cell. No choices. No questions. Only next.

### RULE
ALL code is C. Python is dead. libcurl + jansson + sqlite3 sanctioned.
Dashboard: C HTTP server (44KB ELF, 1.1MB RAM). No Flask. No Python.

### EXECUTE
Build. Fix. Close. C ONLY. PENDING = NOT MONEY.

### UPDATE
Vault after every close -- State after every batch -- Plan when direction shifts
Payout when value moves -- README when grid moves -- Banner when grid moves

### PUSH -> LOOP
One repo. One grid. One walkway. Agent reads, picks, closes, updates, pushes.
There is no time. The agent does not stop. The vault is the record.

### DIRECTORY
Vault: ~/.hermes/mind-palace/
Grid:  money-room-vault.md (T/P/R/C/E rows, 300 cells)
Repo:  waefrebeorn/money-room
Banner: waefrebeorn.github.io/money-room
Memory: ~/.hermes/memories/ (skill + user preference persistence)
